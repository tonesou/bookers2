<h1>画像投稿フォーム</h1>
  <%= form_with model: @book do |f| %>
    <h4>画像</h4>
      <%= f.file_field :image, accept: "image/*" %>
    <p>Title</p>
      <%= f.text_field :shop_name %>
    <p>Opinion</p>
      <%= f.text_area :caption %>
    <%= f.submit '投稿' %>
  <% end %>